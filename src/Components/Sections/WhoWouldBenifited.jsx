import SectionHeader from "../Shared/SectionHeader";
import SectionWrapper from "../Shared/SectionWrapper";

const WhoWouldBenifited = () => {
  return (
    <section>
      <SectionWrapper>
        <SectionHeader title="Who Would be Benifited" />
      </SectionWrapper>
    </section>
  );
};

export default WhoWouldBenifited;
